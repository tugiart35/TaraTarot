diff --git a/src/app/api/cards/[locale]/[slug]/route.ts b/src/app/api/cards/[locale]/[slug]/route.ts
index 1234567..abcdefg 100644
--- a/src/app/api/cards/[locale]/[slug]/route.ts
+++ b/src/app/api/cards/[locale]/[slug]/route.ts
@@ -1,5 +1,6 @@
 import { NextRequest, NextResponse } from 'next/server';
 import { CardData } from '@/features/tarot-cards/lib/card-data';
+import { logger } from '@/lib/logger';
 
 export async function GET(
   _request: NextRequest,
@@ -77,7 +78,10 @@ export async function GET(
       { status: 200 }
     );
   } catch (error) {
-    console.error('Error in GET /api/cards/[locale]/[slug]:', error);
+    logger.error('API error in cards endpoint', error, {
+      action: 'GET /api/cards/[locale]/[slug]',
+      resource: `${locale}/${slug}`
+    });
 
     return NextResponse.json(
       {

